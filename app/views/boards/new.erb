<div class="d-flex align-items-center">
  <h1>掲示板作成<h1>
  <div class="ms-auto boards__linkBox">
    <%= link_to '掲示板に戻る', boards_path, class: 'btn btn-outline-dark' %>
  </div>
</div>
<%= form_with model: @board do |form| %>
  <div class="mb-3">
    <%= form.label :author_name, '名前' %>
    <%= form.text_field :author_name, class: 'form-control' %>
  </div>
  <div class="mb-3">
    <%= form.label :title, 'タイトル' %>
    <%= form.text_field :title, class: 'form-control' %>
  </div>
  <div class="mb-3">
    <%= form.label :body, '本文' %>
    <%= form.text_area :body, class: 'form-control', row: 10 %>
  </div>
  <%= form.submit '保存', class: 'btn btn-primary' %>
<% end %>